{
  "name": "dio-bob-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server para o projeto DIO + IBM Bob — expõe trilhas, desafios e certificados",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "dio-bob-mcp": "./build/index.js"
  },
  "files": ["build"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "start:http": "MCP_TRANSPORT=http node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
